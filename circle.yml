machine:
  services:
    - docker

# test:
#   override:
#     - docker run -d strund3r/attack-shell; sleep 10
#     - curl --retry 10 --retry-delay 5 -v http://localhost
#
# deployment:
#   hub:
#     branch: master
#     commands:
#       - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#       - docker push strund3r/attack-shell
